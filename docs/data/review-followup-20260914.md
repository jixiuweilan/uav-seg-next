# 2026-09-14：部分人工复核接收与后续核实

## 已完成的接收工作

Faith 提交旧英文版导出的正式复核记录。开发端仅检查已存在的 JSON 清单和记录，
没有访问官方原图、重新筛查、修改记录或移动数据划分。

- 111条：26条确认、85条排除。199个候选已填写91个，20个对照全部填写。
- 候选剩余108条未填写；初始筛查另有492个合格候选因首批上限未纳入。
- 必填字段、时间戳、样本与关系编号、manifest/screen身份通过现有工具校验。
- 按原确认关系推导16个连通组，其中8个跨训练/验证，未发现组内排除矛盾。
  这是原判断的计算结果，不等于图像证据已独立核实。
- 原文件 SHA-256 为
  `45249800e5bdb3075aa897f88be4e2a0563760a0f64e45cf961bf217961c5a5a`。
  原文件与工作清单留在被忽略的本地目录，不上传公开仓库。

## 已收到的口径澄清

Faith 在10:40回复“原始缩放图”；10:41补充
“并非无法确定，只是就这张图片难以判断具体地点，但是可以分辨”。

因此不能把“不知道具体地点”直接解释为“无法判断图像关系”，
不因措辞自动将排除改为存疑。73条理由为“无”的原记录保留，
集中澄清不替代逐条图像证据，但也不要求他全部重做。
Faith 在16:00进一步回复“看的小图，没有点开图片”，图片打开方式已澄清。
既有判断基于列表小图，作为初步判断保留，不能视为原图核实完成。
原先分给 Faith 的新版中文体验要求已撤销，已有旧版成果继续接收。

## 下一轮材料已经整理

本地跟进清单第二版包含22条关系、35张去重后的原图需求：

1. 第一优先级：8个跨划分组的15条确认连接，包含组内完整的传递连接链。
2. 第二优先级：7条对照确认、依据歧义或旁述关系。
   其中一条仅从另一条理由中提及的编号整理而来，没有被当作正式人工决定。
3. 每张待准备原图均列出数据集相对路径、审计文件哈希与参考划分。
   工作表的新判断、依据、复核人与时间列均为空，等待实际核实后填写。
4. 全部111条原判断另存带提示的副本；提示不改变判断状态、不构成正式修订。

一条对照关系被人工确认。若原图核实成立，它是当前筛查遗漏的线索，
但不能由单例估计整体召回率或立即调整阈值。
“摄像头原因”只保留为原复核人的解释，当前没有核验设备或成像原因。

## 分散存储下的执行方式

负责人已明确：组员各自持有完整官方数据，数据分散存储。此前将开发端没有数据
推断为组员缺少原图，并要求等待集中准备与传输，是开发代理的错误假设，现予撤销。
无需指定中心数据工作站、收集原图或让组员重新下载数据。

复核人直接按清单编号和相对路径打开自己电脑上的原图。文件哈希用于自动核对
本次涉及的35张图是否与审计批次一致，不要求手工逐项比对或重跑全量审计。
各人数据根目录可以不同，文件身份和样本编号应对应同一批次。
若出现具体的缺图、哈希不一致或打不开问题，再针对该问题排查，不预设所有人受阻。
22条关系的针对性图像判断由无怨无悔承担，替代原说明练习；Faith 不重做旧批次。
[本地原图复核页](../local-original-review.md)由负责人转交，自动按清单核对本地文件，
显示原图并导出回传 JSON。原图留在各自本机，不要求手工比对哈希或整理 CSV。

具体样本与当前工作清单位于本地 `.local/faith-followup-20260914/v3/`，第二版保留作历史记录。
其中的 CSV 保留为备用工作表，当前任务使用 HTML 导出 `scene-followup-decisions`。
两者都不是可直接输入分组工具的决定文件。
回执需由开发代理核对差异并按人工确认生成新版本，保留原版。
第二版中的“等待数据机器准备与传输”步骤已由本节替代；本地第三版同步修正流程，
关系清单与原判断不变。本轮提交接收情况见下节；开发端仍没有读取官方原图。
未核实前不改划分、不删除样本、不标记为训练放行；旧说明练习不再单独追加给无怨无悔。

## 17:10：22条原图复核记录已接收

无怨无悔17:04提交文件，17:10补发完整 JSON 文本。
开发代理保存消息原文并还原 JSON，校验22条均通过，未遗漏本轮关系。
这是从消息文本接收的记录，不声称与未成功下载的附件逐字节相同。

- 18条确认、1条排除、3条存疑；复核人、含时区时间、查看声明和两图哈希均符合批次契约。
- 对照旧记录，18条状态一致、3条从排除变为存疑，另有1条从原旁述线索变为明确确认。
  三条存疑均以一侧模糊为由，原来的“摄像头原因”仍不是已验证的成因。
- 8个跨划分组的15条连接全部再次被确认为相同。
- 19条确定判断的理由仅为“一样”“差不多”“没问题”“不一样”或“都糊，但是一样”，
  尚未写出可追溯的固定地物依据。格式通过不等于图像判断已由开发代理独立核验。

所有成果保留，不要求重填22条。当前仅对一条模糊图确认、一条“差不多”的跨划分确认，
请求补充具体图像依据；不修改原判断，也不靠两条例子替代其余关系的逐条证据。
复核文件已收齐，接收当时仅余这两点依据澄清；后续负责人已决定结束追问，见下文。

另做了只读情景计算：若接受全部新声明，共112条关系、15个连通组、8个跨划分组，
没有组内排除矛盾。新增连接会合并两个旧组。该计算不是正式合并或划分变更。
原111条文件未改动，原图和标签未读取或改动；接收回执与情景报告仅保存在本地忽略目录。

## 负责人确认：三组存疑关系均为不同场景

负责人针对刚列出的三组存疑关系明确回复“可以确定显然不是同一场景”。
这三组当前结论记为排除，作为单独的版本化修订保留，不覆盖组员的原始提交。
修订后的本轮状态汇总为18条确认、4条排除、0条存疑。

该确认来自负责人对话，没有被记作开发代理已独立查看原图，也不补写未提供的地物依据。
本次只解决三组存疑关系，不改变确认连接或数据划分。

## 负责人决定结束追加追问，进入数据准备

负责人随后明确要求不再追问两组图像的细节，继续推进项目。本轮接收据此收尾，
不再向组员追加任务；保留已有判断和证据局限，不写成已独立核验图像。

工作版本保留旧111条，后续21条经负责人解决存疑后与旧状态一致，仅补充证据而不改写
原记录身份；新增1条原旁述线索的人工确认。工作版本共112条，27确认、85排除。
其中两处理由简略的确认也保留，保守地让关联图像不跨训练/验证。

已生成整组候选划分并完成 CPU 图像/标签变换代码，结果与限制见
[基线数据准备](../baseline-data.md)。这是新的候选清单，不覆盖参考划分或移动原始文件。
